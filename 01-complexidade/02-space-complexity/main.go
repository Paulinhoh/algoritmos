/*
O que é "space complexity":
	É a medida do espaço em memória (RAM) necessário para executar um programa ou algoritmo. Ajuda a avaliar a eficiência de um algoritmo em termos de uso de memória. Alogoritmos que consomem menos memória são preferíveis.

	De forma simples, é a possivel quantia de memória que um algoritmo utiliza para terminar sua execução.

Exemplo para simplificar:
	A space complexity é frquentemente expressa como uma função do tamanho da entrada. Por exemplo   O(1) significa que o espaço usado é constante, não importa o tamanho da entrada. O(n) significa que o espaço aumenta linearmente com o tamanho de entrada.

	Imagine uma lista de números. Se você copiar todos esses números em uma nova lista, estará usando espaço adicional igual ao tamanho da lista original. Isso teria uma space comlexity de    O(n).
*/

package main

func main() {
}
